import Vue from 'vue'
import Vuex from 'vuex'
import comsOption from './comsOption/comsOption'

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
      comsOption
  }
})
